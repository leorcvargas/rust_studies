# Variables

## Variables and Mutability

Variables can be declared with `let`. By default all variables are immutable.
Mutability can be achieved by using `let mut` when declaring a variable. A
Rust program wont compile if there is a direct reassignment to a immutable
variable.

## Declaring constants

TBD
